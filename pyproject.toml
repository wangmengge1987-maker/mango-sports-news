[tool.ruff]
# 代码行长度
line-length = 100

# 需要检查的文件
target-version = "py312"

# 忽略规则
ignore = [
    "E501",  # 行长度（已在 line-length 限制）
]

[tool.ruff.lint]
# 启用规则
select = [
    "E",   # pycodestyle 错误
    "W",   # pycodestyle 警告
    "F",   # pyflakes
    "I",   # isort（导入排序）
]
